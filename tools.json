[
  {
    "type": "function",
    "function": {
      "name": "left_click",
      "description": "Perform a single left mouse click at the specified coordinates.",
      "parameters": {
        "type": "object",
        "properties": {
          "x": {
            "type": "integer",
            "description": "The absolute X coordinate."
          },
          "y": {
            "type": "integer",
            "description": "The absolute Y coordinate."
          }
        },
        "required": [
          "x",
          "y"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "right_click",
      "description": "Perform a single right mouse click at the specified coordinates.",
      "parameters": {
        "type": "object",
        "properties": {
          "x": {
            "type": "integer",
            "description": "The absolute X coordinate."
          },
          "y": {
            "type": "integer",
            "description": "The absolute Y coordinate."
          }
        },
        "required": [
          "x",
          "y"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "double_click",
      "description": "Perform a double left mouse click at the specified coordinates.",
      "parameters": {
        "type": "object",
        "properties": {
          "x": {
            "type": "integer",
            "description": "The absolute X coordinate."
          },
          "y": {
            "type": "integer",
            "description": "The absolute Y coordinate."
          }
        },
        "required": [
          "x",
          "y"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "scroll",
      "description": "Scroll the mouse wheel at a specific location.",
      "parameters": {
        "type": "object",
        "properties": {
          "x": {
            "type": "integer",
            "description": "The absolute X coordinate."
          },
          "y": {
            "type": "integer",
            "description": "The absolute Y coordinate."
          }
        },
        "required": [
          "x",
          "y"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "scroll_down",
      "description": "Scroll the screen content downwards at the specified coordinates.",
      "parameters": {
        "type": "object",
        "properties": {
          "x": {
            "type": "integer",
            "description": "The absolute X coordinate."
          },
          "y": {
            "type": "integer",
            "description": "The absolute Y coordinate."
          }
        },
        "required": [
          "x",
          "y"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "scroll_up",
      "description": "Scroll the screen content upwards at the specified coordinates.",
      "parameters": {
        "type": "object",
        "properties": {
          "x": {
            "type": "integer",
            "description": "The absolute X coordinate."
          },
          "y": {
            "type": "integer",
            "description": "The absolute Y coordinate."
          }
        },
        "required": [
          "x",
          "y"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "drag_to",
      "description": "Click and hold, then drag following a trajectory defined by a list of coordinates.",
      "parameters": {
        "type": "object",
        "properties": {
          "coord_list": {
            "type": "array",
            "description": "A list of (x, y) coordinate pairs defining the trajectory.",
            "items": {
              "type": "array",
              "items": {
                "type": "integer"
              },
              "minItems": 2,
              "maxItems": 2
            }
          }
        },
        "required": [
          "coord_list"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "press_key",
      "description": "Press and quickly release a single key.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The name of the key (e.g., 'w', 'enter', 'esc', 'space')."
          }
        },
        "required": [
          "key"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "key_down",
      "description": "Press and hold down a specific key.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The name of the key to hold down."
          }
        },
        "required": [
          "key"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "key_up",
      "description": "Release a key that is currently being held down.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The name of the key to release."
          }
        },
        "required": [
          "key"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "wait",
      "description": "Pause execution for a short period to allow for animations or loading screens to complete.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  }
]